import React from 'react';
import './Loader.css';

const Loader = () => {
    return (
        <div className="loader">
            <p>Cargando...</p>
            <img src="ruta/al/tu/loader.gif" alt="Loader" />
        </div>
    );
};

export default Loader;
